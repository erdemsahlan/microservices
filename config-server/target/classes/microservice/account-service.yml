server:
  port: 8501

spcloud:
  cassandra:
    contact:
      point: localhost
    port : 9042
    keyspace:
      name: springcloud

    username : cassandra
    password : cassandra

eureka:
  client:
    instance:
      hostname: localhost
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/